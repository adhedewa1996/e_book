import 'package:flutter/material.dart';

class ReadingMode extends StatelessWidget {
  const ReadingMode({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Reading Mode'));
  }
}
